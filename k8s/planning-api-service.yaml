apiVersion: v1
kind: Service
metadata:
  name: planning-api
  namespace: uav-platform
  labels:
    app.kubernetes.io/name: planning-api
    app.kubernetes.io/part-of: uav-platform
    app.kubernetes.io/component: microservice
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: planning-api
  ports:
    - name: http
      port: 8086
      targetPort: 8086
      protocol: TCP
