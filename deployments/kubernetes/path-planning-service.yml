apiVersion: apps/v1
kind: Deployment
metadata:
  name: path-planning-service
  namespace: uav-platform
  labels:
    app: path-planning-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: path-planning-service
  template:
    metadata:
      labels:
        app: path-planning-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: path-planning
          image: trae-path-planning:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: uav-platform-config
          env:
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: uav-platform-config
                  key: SPRING_DATASOURCE_URL_PATH_PLANNING
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: spring.datasource.password
                  name: uav-platform-secrets
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: spring.datasource.password
                  name: uav-platform-secrets
            - name: PYTHON_TIMEOUT
              valueFrom:
                configMapKeyRef:
                  name: uav-platform-config
                  key: PYTHON_TIMEOUT_PLANNING
            - name: JAVA_OPTS
              valueFrom:
                configMapKeyRef:
                  name: uav-platform-config
                  key: JAVA_OPTS_PATH_PLANNING
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "1"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 30
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
---
apiVersion: v1
kind: Service
metadata:
  name: path-planning-service
  namespace: uav-platform
spec:
  selector:
    app: path-planning-service
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP
