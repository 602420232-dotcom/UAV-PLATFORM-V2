apiVersion: apps/v1
kind: Deployment
metadata:
  name: meteor-forecast-service
  namespace: uav-platform
  labels:
    app: meteor-forecast-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: meteor-forecast-service
  template:
    metadata:
      labels:
        app: meteor-forecast-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: meteor-forecast
          image: trae-meteor-forecast:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: uav-platform-config
          env:
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: uav-platform-config
                  key: SPRING_DATASOURCE_URL_METEOR_FORECAST
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: spring.datasource.password
                  name: uav-platform-secrets
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: spring.datasource.password
                  name: uav-platform-secrets
            - name: PYTHON_TIMEOUT
              valueFrom:
                configMapKeyRef:
                  name: uav-platform-config
                  key: PYTHON_TIMEOUT_FORECAST
            - name: JAVA_OPTS
              valueFrom:
                configMapKeyRef:
                  name: uav-platform-config
                  key: JAVA_OPTS_METEOR_FORECAST
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "1"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 30
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
---
apiVersion: v1
kind: Service
metadata:
  name: meteor-forecast-service
  namespace: uav-platform
spec:
  selector:
    app: meteor-forecast-service
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP
