{{- if .Values.riskApi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: risk-api
  namespace: {{ .Values.namespace.name }}
  labels:
    app.kubernetes.io/name: risk-api
    app.kubernetes.io/part-of: uav-platform
    app.kubernetes.io/component: microservice
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
spec:
  replicas: {{ .Values.riskApi.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: risk-api
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: risk-api
        app.kubernetes.io/part-of: uav-platform
        app.kubernetes.io/component: microservice
        app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.riskApi.containerPort }}"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - risk-api
                topologyKey: kubernetes.io/hostname
      containers:
        - name: risk-api
          image: {{ .Values.global.imageRegistry }}/{{ .Values.riskApi.image.repository }}:{{ .Values.riskApi.image.tag | default .Values.global.imageTag }}
          imagePullPolicy: {{ .Values.riskApi.image.pullPolicy | default .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.riskApi.containerPort }}
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: SPRING_PROFILES_ACTIVE
            - name: NACOS_SERVER_ADDR
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: NACOS_SERVER_ADDR
            - name: SPRING_DATASOURCE_URL
              value: {{ printf "jdbc:mysql://%s:%s/uav_platform?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true" .Values.global.mysql.host .Values.global.mysql.port }}
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: REDIS_HOST
            - name: REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: REDIS_PORT
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-secrets
                  key: REDIS_PASSWORD
            - name: SPRING_DATA_REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: SPRING_DATA_REDIS_HOST
            - name: SPRING_DATA_REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: SPRING_DATA_REDIS_PORT
            - name: ALGORITHM_ENGINE_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: ALGORITHM_ENGINE_URL
            - name: JAVA_OPTS
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: JAVA_OPTS
          resources:
            {{- toYaml .Values.riskApi.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.riskApi.containerPort }}
            initialDelaySeconds: {{ .Values.riskApi.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.riskApi.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.riskApi.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.riskApi.livenessProbe.failureThreshold }}
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.riskApi.containerPort }}
            initialDelaySeconds: {{ .Values.riskApi.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.riskApi.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.riskApi.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.riskApi.readinessProbe.failureThreshold }}
            successThreshold: 1
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: risk-api
  namespace: {{ .Values.namespace.name }}
  labels:
    app.kubernetes.io/name: risk-api
    app.kubernetes.io/part-of: uav-platform
    app.kubernetes.io/component: microservice
spec:
  type: {{ .Values.riskApi.service.type }}
  selector:
    app.kubernetes.io/name: risk-api
  ports:
    - name: http
      port: {{ .Values.riskApi.service.port }}
      targetPort: {{ .Values.riskApi.containerPort }}
      protocol: TCP
{{- end }}
