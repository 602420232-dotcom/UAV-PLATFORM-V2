{{- if .Values.weatherApi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-api
  namespace: {{ .Values.namespace.name }}
  labels:
    app.kubernetes.io/name: weather-api
    app.kubernetes.io/part-of: uav-platform
    app.kubernetes.io/component: microservice
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
spec:
  replicas: {{ .Values.weatherApi.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: weather-api
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: weather-api
        app.kubernetes.io/part-of: uav-platform
        app.kubernetes.io/component: microservice
        app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.weatherApi.containerPort }}"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - weather-api
                topologyKey: kubernetes.io/hostname
      containers:
        - name: weather-api
          image: {{ .Values.global.imageRegistry }}/{{ .Values.weatherApi.image.repository }}:{{ .Values.weatherApi.image.tag | default .Values.global.imageTag }}
          imagePullPolicy: {{ .Values.weatherApi.image.pullPolicy | default .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.weatherApi.containerPort }}
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: SPRING_PROFILES_ACTIVE
            - name: NACOS_SERVER_ADDR
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: NACOS_SERVER_ADDR
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: REDIS_HOST
            - name: REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: REDIS_PORT
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-secrets
                  key: REDIS_PASSWORD
            - name: SPRING_DATA_REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: SPRING_DATA_REDIS_HOST
            - name: SPRING_DATA_REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: SPRING_DATA_REDIS_PORT
            - name: ALGORITHM_ENGINE_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: ALGORITHM_ENGINE_URL
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: KAFKA_MOCK
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: KAFKA_MOCK
            - name: JAVA_OPTS
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: JAVA_OPTS
          resources:
            {{- toYaml .Values.weatherApi.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.weatherApi.containerPort }}
            initialDelaySeconds: {{ .Values.weatherApi.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.weatherApi.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.weatherApi.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.weatherApi.livenessProbe.failureThreshold }}
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.weatherApi.containerPort }}
            initialDelaySeconds: {{ .Values.weatherApi.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.weatherApi.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.weatherApi.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.weatherApi.readinessProbe.failureThreshold }}
            successThreshold: 1
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: weather-api
  namespace: {{ .Values.namespace.name }}
  labels:
    app.kubernetes.io/name: weather-api
    app.kubernetes.io/part-of: uav-platform
    app.kubernetes.io/component: microservice
spec:
  type: {{ .Values.weatherApi.service.type }}
  selector:
    app.kubernetes.io/name: weather-api
  ports:
    - name: http
      port: {{ .Values.weatherApi.service.port }}
      targetPort: {{ .Values.weatherApi.containerPort }}
      protocol: TCP
{{- end }}
