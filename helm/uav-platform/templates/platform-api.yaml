{{- if .Values.platformApi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-api
  namespace: {{ .Values.namespace.name }}
  labels:
    app.kubernetes.io/name: platform-api
    app.kubernetes.io/part-of: uav-platform
    app.kubernetes.io/component: microservice
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
spec:
  replicas: {{ .Values.platformApi.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: platform-api
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: platform-api
        app.kubernetes.io/part-of: uav-platform
        app.kubernetes.io/component: microservice
        app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.platformApi.containerPort }}"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - platform-api
                topologyKey: kubernetes.io/hostname
      containers:
        - name: platform-api
          image: {{ .Values.global.imageRegistry }}/{{ .Values.platformApi.image.repository }}:{{ .Values.platformApi.image.tag | default .Values.global.imageTag }}
          imagePullPolicy: {{ .Values.platformApi.image.pullPolicy | default .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.platformApi.containerPort }}
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: SPRING_PROFILES_ACTIVE
            - name: NACOS_SERVER_ADDR
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: NACOS_SERVER_ADDR
            - name: SPRING_DATASOURCE_URL
              value: {{ .Values.platformApi.datasourceUrl | default (printf "jdbc:mysql://%s:%s/uav_platform?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true" .Values.global.mysql.host .Values.global.mysql.port) }}
            - name: MYSQL_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: MYSQL_HOST
            - name: MYSQL_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: MYSQL_PORT
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-secrets
                  key: MYSQL_PASSWORD
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: REDIS_HOST
            - name: REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: REDIS_PORT
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-secrets
                  key: REDIS_PASSWORD
            - name: SPRING_DATA_REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: SPRING_DATA_REDIS_HOST
            - name: SPRING_DATA_REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: SPRING_DATA_REDIS_PORT
            - name: ALGORITHM_ENGINE_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: ALGORITHM_ENGINE_URL
            - name: KAFKA_MOCK
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: KAFKA_MOCK
            - name: JAVA_OPTS
              valueFrom:
                configMapKeyRef:
                  name: {{ include "uav-platform.fullname" . }}-config
                  key: JAVA_OPTS
          resources:
            {{- toYaml .Values.platformApi.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.platformApi.containerPort }}
            initialDelaySeconds: {{ .Values.platformApi.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.platformApi.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.platformApi.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.platformApi.livenessProbe.failureThreshold }}
            successThreshold: 1
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.platformApi.containerPort }}
            initialDelaySeconds: {{ .Values.platformApi.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.platformApi.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.platformApi.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.platformApi.readinessProbe.failureThreshold }}
            successThreshold: 1
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: platform-api
  namespace: {{ .Values.namespace.name }}
  labels:
    app.kubernetes.io/name: platform-api
    app.kubernetes.io/part-of: uav-platform
    app.kubernetes.io/component: microservice
spec:
  type: {{ .Values.platformApi.service.type }}
  selector:
    app.kubernetes.io/name: platform-api
  ports:
    - name: http
      port: {{ .Values.platformApi.service.port }}
      targetPort: {{ .Values.platformApi.containerPort }}
      protocol: TCP
{{- end }}
